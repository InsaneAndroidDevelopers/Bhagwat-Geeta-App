<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bhagvatgeetaapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gita_app_icon1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gita_app_icon1"
        android:supportsRtl="true"
        android:theme="@style/Theme.BhagvatGeetaApp">
        <activity android:name=".AboutActivity" />
        <activity android:name=".VerseActivity" />
        <activity android:name=".AllVerse" />
        <activity android:name=".AllChapters" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>